/*This plugin can be used as an immediate init plugin when you are loading
boomerang asynchronously: */

if (BOOMR.window.BOOMR_GLOBAL_CONFIG !== undefined) {
  BOOMR.init(BOOMR.window.BOOMR_GLOBAL_CONFIG);
}
